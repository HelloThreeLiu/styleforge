# AI 评分：20260821-darkroom-local-llm-settings — 暗房 Darkroom · 本地大模型运行器设置页

| 项 | 值 |
|---|---|
| 总分 | ★4.0 |
| 评分时间 | 2026-08-21T14:51:00+08:00 |
| 评分代理 | styleforge-rater（zcode / GLM-5.3） |
| 评分依据 | screenshot.png（像素级采样核验：#08090a 画布/近白文本/稀疏靛蓝与 emerald 状态点与实现一致）· index.html · DESIGN.md（lineage: linear.app，种子风格） |

## 五维得分

| 维度 | 权重 | 得分 | 加权 | 关键证据（摘要） |
|---|---|---|---|---|
| 风格保真度 | 30% | 4.0 | 1.20 | 三/四级表面、文本四层、半透明白描边、按钮微透明底、510/590 字重、-1.056px@48、cv01/ss03、命令面板逐条对上 DESIGN.md；但危险区引入暖红违反 §7 "不得在 UI chrome 引入暖色" 且 notes 未声明 |
| 视觉工艺 | 25% | 4.5 | 1.125 | 行式设置布局对齐严格、mono 数值、两步确认 3 秒复位、⌘K 全键盘导航、scrollspy；瑕疵为 768 档 `.row .ctl` 死代码与原生 range 仅 accent-color 定制 |
| 完整性与内容 | 20% | 5.0 | 1.00 | 页头 + 六个设置分区 + 保存条 + 页脚齐全；本地模型运行器文案（Qwen3-14B Q4_K_M、RTX 4070 显存、快捷键表）真实密度高；五档断点逐一成立 |
| 创意增值 | 15% | 4.5 | 0.675 | Linear 工程气质与本地 LLM 运行器互相成就；可用的 ⌘K 命令面板与"断网照常工作/对话只属于硬盘"叙事是记忆点 |
| 工程合规 | 10% | 4.5 | 0.45 | 底线全满足，notes 翔实；但 §三"无其他偏离"与危险区红不符，略有水分 |
| **加权和** | | | **4.45** | |

最终分 **4.0**（向下取半；门禁：无触发）。

## 分维度评语

### 风格保真度
对 367 行的 linear.app DESIGN.md 完成了高精度复现：`#08090a` 画布（截图像素核验占 96.8% 中位命中）、文本四层 `#f7f8f8/#d0d6e0/#8a8f98/#62666d`、半透明白描边 `.05/.08`、按钮底 `rgba(255,255,255,.02–.05)`、签名字重 510 与 590（全页无一处 700）、`h1` `clamp(30px,4.4vw,48px)/510/1.00/-1.056px` 精确等于 Display 行、全局 `font-feature-settings:"cv01","ss03"`、命令面板（`#191a1b` + `.08` 描边 + 12px 圆角 + 五层级联阴影 + 13px/510 列表）几乎逐条复刻 §9 示例组件；靛蓝三值只用于 CTA/激活/选中，成功绿只做状态指示。扣分点明确：危险区引入 `#ee1d36` 系暖红（30% 描边 + 3% 底 + 红字按钮），而 DESIGN.md 色板无红且 §7 明令"Don't introduce warm colors into the UI chrome"——notes §三以"无其他偏离"收尾，未覆盖此点；另有 `--panel #0f1011`、`--surface-hover #28282c` 定义未用、`.desc` 缺 -0.165px 字距等小项。
### 视觉工艺
"亮度堆叠 + 月光线框"的 Linear 质感成立：设置行标签/说明/控件三段对齐严格，模型卡 mono 元数据（8.4 GB · Q4_K_M）与 kbd 徽章细节到位；两步确认（"再点一次确认清除"3 秒复位）与 ⌘K 面板（实时过滤、↑↓ 循环高亮、↵ 跳转、Esc 关闭）是真实可用的交互打磨；480 档 toggle 放大 44px、模型卡 meta 换行，触控考虑周到。瑕疵：768 档 `.row .ctl` 样式是死代码（HTML 中并无 .ctl 包裹，控件纵排后未做占宽右对齐），原生 range 滑块仅靠 `accent-color` 定制，与 Linear 自绘滑块有差距。
### 完整性与内容
设置页五件套齐全（导航、带状态 pill 的页头、六个分区、保存条、页脚）。文案优秀且自洽："一切推理都在这台机器上完成。断网之后，暗房照常工作"、显存 8.7/12 GB · RTX 4070、`~/darkroom/models` 路径、⌘⇧M 切换模型——工程细节可信，无占位。992/768/480/375 四档均显式处理（375 标题 30px 达底线）。
### 创意增值
从计划的"漫画阅读器"改选"本地 LLM 运行器"是正确的判断——Linear 的极简工程语言与本地推理工具的气质互相成就；命令面板不只是摆设而是全键盘可用，"你的对话只属于你的硬盘"把隐私分区文案变成了品牌叙事。属"有清晰的本地化适配和亮点"的 4.5 档。
### 工程合规
单文件自包含（仅 Google Fonts Inter 可变字体）、字体回退栈与 mono 回退链完整、无外部图片。notes 三节翔实，token 表可回查；但 §三"无其他偏离"的总结与危险区红的存在不符，构成轻微的声明水分。

## 门禁检查

- [x] 无外部图片
- [x] 三档断点齐全
- [x] 字体系统回退
- [x] notes 与实现一致

## 一句话结论

踩在沉淀线上的高保真 study，命令面板与题材选择是亮点；最该修的一件事是危险区暖红既违反 linear"无暖色"Don't 又未在 notes 声明——应补声明或改用中性/靛蓝处理。

> 本评分为 AI 代评初稿，策展人可在 Web 平台随时改分；人类改分后以人类评分为准，本文件保留作审计记录。
